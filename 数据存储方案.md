# SwimTrack 数据存储方案（GitHub + Railway 版）

> 目标：免费、数据不丢失、上传下载顺畅、账号多设备同步。
> 当前实现：单进程 Node 服务（`server.js`）同进程托管前端与 `/api`，数据落在**持久化卷**上的单个 JSON 文件。

---

## 1. 主存储：单文件 JSON（`store.json`）

| 项 | 说明 |
|---|---|
| 位置 | `${DATA_DIR}/store.json`，Railway 上建议 `DATA_DIR=/data`（挂载持久化卷） |
| 内容 | 全部账号、成绩记录、积分、签到、盲盒等都在这一个文件里（结构见 `api.js` 的 `normalizeStore`） |
| 写入 | 每次变更后 **200ms 防抖** 落盘（`saveStore` 中的 `setTimeout`） |
| 读取 | 进程启动时 `loadStore()` 一次性读入内存，所有请求在内存中处理，性能好 |

**优点**：零外部依赖、和现有代码完全兼容、备份/迁移只需复制一个文件。

---

## 2. 防丢失三层（核心）

### L1 — 持久化卷（跨重启 / 重部署保留）
Railway 的文件系统默认是临时的，**每次重新部署会被重置**，所以必须把 `/data` 挂成 **Volume**。
- 在 Railway 项目里 Add Volume，挂载路径填 `/data`
- 设环境变量 `DATA_DIR=/data`
- `api.js` 已具备 `fs.mkdirSync(DATA_DIR, { recursive: true })`，挂载后自动建目录
- 卷在重启、重部署、回滚时都保留，是数据不丢失的第一道保险

### L2 — 本地滚动备份（防误删 / 文件损坏）
每次 `saveStore` 落盘时，除写 `store.json` 外还会在 `${DATA_DIR}/backups/` 留一份**带时间戳**的副本，并自动保留最近 **7 份**：

```
/data/backups/store-2026-08-17T02-31-14-123Z.json
/data/backups/store-2026-08-17T03-12-08-456Z.json
...
```

误删或 `store.json` 损坏时，把最新一份备份 rename 回去即可恢复。

### L3 — 原子写（防半截文件）
写入不是直接覆盖，而是：
1. 先写 `store.json.tmp`
2. 再 `rename` 成 `store.json`（操作系统级别的原子操作）

这样即使写入中途进程被 Railway 重启/杀掉，也不会留下半截 JSON 导致下次启动解析失败。

### （可选）L4 — 异地备份
如需对抗"卷被误删"的极端情况，可加一个 GitHub Action（cron 每天一次）：
- 定时 `curl -H "Authorization: Bearer <管理Token>" https://<你的域名>/api/export`
- 把返回的全量 JSON commit 到一个**私有**仓库/分支

> 当前版本已内置 `/api/export`（导出全量），接一个定时任务即可，无需改代码。

---

## 3. 并发与一致性

- Railway 默认**单实例**运行，`saveStore` 的 200ms 防抖 + 单线程事件循环保证写入串行，**无并发写冲突**。
- 内存是唯一数据源，磁盘只是持久化镜像；不会出现"两个进程各写各的"导致数据分叉。
- 若将来扩容到多实例，需要引入真正的数据库（Postgres）或把写请求路由到单一实例——目前规模不需要。

---

## 4. 容量与媒体文件

- `store.json` 是文本，几千条成绩记录也就几百 KB ~ 几 MB，远低于卷的容量上限。
- **若用户上传图片**（泳姿照、证书等）：
  - 建议单独目录 `${DATA_DIR}/uploads/`，不要塞进 `store.json`（会让单文件膨胀、解析变慢）。
  - 卷容量有限，前端应限制单图大小（如 ≤2MB）并压缩。
  - 更省空间的长期方案：把图片转存到外部图床 / 对象存储（如 Cloudflare R2、GitHub LFS），`store.json` 里只存 URL。

---

## 5. 多设备 / 账号同步

- 单实例集中存储天然支持多设备共享：手机、平板、电脑登录同一账号，数据都在同一份 `store.json` 里。
- 离线场景：`/api/sync` 端点支持客户端**全量合并**（客户端带本地改动上来，服务端按 `deletedIds` + 时间戳合并），换设备重登即同步。
- 多端同时登录：账号支持 `tokens` 数组（一个账号可同时持有多个设备 Token），已在 `normalizeStore` 中兼容。

---

## 6. 日常运维

| 操作 | 做法 |
|---|---|
| 查看数据 | Railway 控制台 → 项目 → Volumes → 浏览 `/data/store.json` |
| 手动备份 | 复制 `/data/store.json` 到本地即可 |
| 误删恢复 | 取 `/data/backups/` 最新一份 rename 回 `store.json`，重启服务 |
| 迁移到新环境 | 把 `store.json` 拷到新卷的 `/data/` 下，启动即生效 |
| 重置数据 | 删 `/data/store.json` 并重启（会回到空存储） |

---

## 7. 与 Cloudflare 方案的数据层对比

| 维度 | Railway（当前） | Cloudflare（旧方案） |
|---|---|---|
| 存储 | 卷上单 JSON 文件 | D1 边缘 SQLite |
| 备份 | 本地滚动备份 + 可选异地 | R2 每日快照 |
| 一致性 | 单实例串行，简单 | 需处理 D1 并发写 |
| 复杂度 | 低（一个卷） | 高（D1+R2+Token+5 Secret） |
| 免费性 | 需绑卡，额度内≈免费 | 真免费无卡 |

**结论**：当前规模（个人/小团队游泳成绩追踪）下，Railway 单文件 + 卷 + 滚动备份的组合最省心，完全满足"不丢失 + 多设备同步"。
