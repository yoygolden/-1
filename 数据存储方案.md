# SwimTrack 数据存储方案（GitHub + Render + Cloudflare）

## 结论速览
- 主存储：Render 实例本地盘上的单文件 `store.json`（路径由 `DATA_DIR` 控制，默认 `/data/store.json`）。
- 防丢三道防线：
  1. **原子写**：先写 `store.json.tmp` 再 `rename`，避免进程中途被杀产生半截文件。
  2. **本地滚动备份**：每次保存自动留一份带时间戳副本（`/data/backups/`，保留最近 7 份），误删/损坏可回滚。
  3. **每日异地备份 + 启动自愈**：每天 GitHub Action 把整库（`/api/backup`）拉到仓库 `backups/store.json`；Render 重启后若本地为空，自动从 `BOOTSTRAP_URL` 拉回最新备份，**数据等价于不丢**。

## 为什么不直接用持久磁盘
Render **免费档不提供持久磁盘**，文件系统是临时的，每次重新部署会重置。因此数据不能只依赖本地盘——这就是「每日 GitHub 备份 + 启动自愈」存在的意义。若日后升级到付费磁盘，可把 `DATA_DIR` 挂到持久盘，自愈逻辑仍作为额外保险。

## 数据模型（单文件 JSON）
```
store.json
└── users: { [account]: {
      account, nickname, salt, hash,
      tokens: [ ... ]          // 多设备同时登录（最多保留最近 10 个）
      records: [ {id, stroke, category, distance, count, timeMs, date, ...} ]
      checkins: [ {date, ...} ]
      blindBoxes: [ {id, ...} ]
      deletedIds: [ ... ]      // 软删除墓碑，防止其他设备把删掉的记录同步回来
  } }
```
- 积分**全部服务端重算**（`derivePoints` / `deriveCheckinTotal` / `computePoints`），不信任客户端上报值，保证多端一致。
- 删除采用软删除（`deletedIds` 墓碑），同步合并时不会「复活」已删记录。

## 同步与多设备
- 登录发 Bearer Token，写入 `tokens` 数组 → 同一账号可在多设备同时登录。
- `/api/sync`、`/api/profile` 以各自主键（记录 id / 打卡 date / 盲盒 id）合并，返回最新全量，多端拿到一致数据。
- 单实例串行写入，无并发冲突。

## 备份与恢复流程
- **自动备份**：`.github/workflows/backup.yml` 每日 UTC 16:23 调 `/api/backup`（需 `x-backup-token: ${EXPORT_TOKEN}`），把整库提交到 `backups/store.json`。
- **启动自愈**：`api.js: loadStore()` 发现本地为空白且配置了 `BOOTSTRAP_URL` 时，拉取该 raw 文件还原。
- **手动恢复**：对线上 `POST /api/restore`（同 token）发送任意一份 `store.json` 全量即可整体覆盖。

## 配置清单（环境变量）
| 变量 | 作用 | 在哪设 |
|---|---|---|
| `PORT` | 监听端口（Render 自动注入） | 自动 |
| `DATA_DIR` | store.json 目录 | Render：`/data` |
| `EXPORT_TOKEN` | 备份/恢复接口鉴权 | Render 与 GitHub Secret 同值 |
| `BOOTSTRAP_URL` | 启动自愈用的备份 raw 地址 | Render |

## 容量与媒体
- 成绩、打卡均为文本 JSON，单用户数据量极小，单文件可支撑数万条记录无压力。
- 暂无图片/视频附件需求；若未来需要，建议走 Cloudflare R2 或对象存储，不在本方案范围。

## 风险与边界
- 理论上两次自动备份之间的窗口（≤24h）若遇灾难且无可恢复快照，最多丢失当天增量；可把 Cron 频率调高（如每小时）进一步缩小窗口。
- `BOOTSTRAP_URL` 指向公开 raw 时，备份文件对他人可读（含脱敏后的数据，无明文密码——密码以 scrypt 哈希存储）。如介意，请用私有仓库 + 带 PAT 的 raw 地址，或改用方案 A（数据落 Cloudflare R2）。
