# SwimTrack 数据存储方案（Cloudflare Workers + D1，不用 R2）

## 结论速览
- **主存储：Cloudflare D1**（托管 SQLite）。建一张 `accounts` 表，**按账号拆分存储**（每条账号一行），避免单值超过 D1 的 1MB 限制。
- **不用 R2**：SwimTrack 全是结构化文本数据（账号/成绩/打卡/盲盒），D1 比塞 JSON 进 R2 更合适、可查询、可索引；R2 仅在你将来要存用户上传的图片/视频时才需要，现在无此需求。
- **防丢三道防线**：① D1 多副本冗余（Cloudflare 自动）② 每日 `wrangler d1 export` 备份到 GitHub（`backups/store.sql`）③ 旧数据可随时 `POST /api/restore` 整体覆盖。

## 存储结构

```
D1 数据库 swimtrack
└── 表 accounts (
      account TEXT PRIMARY KEY,
      data    TEXT NOT NULL      -- 该账号的完整 JSON：{ account, nickname, salt, hash, tokens, records, checkins, blindBoxes, deletedIds }
    )
```

- 内存模型 `store = { users: { [account]: {...} } }`，与 Node 本地文件版完全一致；`d1store.read()` 把各账号行拼回整份 JSON，`d1store.write(store)` 按账号 upsert 并删除已不存在的账号行。
- 积分**全部服务端重算**（`derivePoints` / `deriveCheckinTotal` / `computePoints`），不信任客户端上报值，保证多端一致。
- 删除采用软删除（`deletedIds` 墓碑），同步合并时不会「复活」已删记录。

## 同步与多设备
- 登录发 Bearer Token，写入 `tokens` 数组（最多保留最近 10 台）→ 同一账号可多设备同时登录（已修复之前「单 token 互相踢下线」的 bug）。
- `/api/sync`、`/api/profile` 以各自主键（记录 id / 打卡 date / 盲盒 id）合并，返回最新全量，多端拿到一致数据。
- D1 作为**唯一真相源**，任一端改动，其他端刷新即见，天然多设备共享。

## 备份与恢复
- **每日自动备份**：`.github/workflows/backup.yml` 每日 UTC 16:23 `wrangler d1 export swimtrack --remote --output=backups/store.sql` 并提交到仓库。
- **整库恢复**：`wrangler d1 execute swimtrack --remote --file=backups/store.sql`（或导入后重新 upsert）。
- **单账号/整体覆盖**：已部署后 `POST /api/restore`（带 `x-backup-token`）发送任意一份完整 `store` JSON 即可。
- **旧本地数据迁移**：`D1_ACCOUNT_ID=... D1_DATABASE_ID=... CF_API_TOKEN=... node tools/migrate-to-d1.js`，把 `data/store.json` 写入 D1。

## 配置清单（环境变量）
| 变量 | 作用 | 在哪设 |
|---|---|---|
| `CLOUDFLARE_ACCOUNT_ID` | Cloudflare 账号 ID | CI Secret / 本地环境变量 |
| `D1_DATABASE_ID` | D1 数据库 ID | CI Secret / 本地环境变量 |
| `CF_API_TOKEN` | Cloudflare API Token（D1+Workers Edit） | CI Secret（deploy/backup）/ 本地迁移用 |

> Workers 运行时通过 `wrangler.toml` 的 `[[d1_databases]]` binding（`DB`）注入，业务代码不感知；未绑定 D1 时自动回退到本地文件存储（Node/server.js），便于本地开发。

## 容量与媒体
- 成绩、打卡均为文本 JSON，单用户数据量极小，D1 免费额度（GB 级）远超家庭使用。
- 暂无图片/视频附件需求；若未来需要，再加 R2 或 Cloudflare Images，作为**可选扩展**，不影响现有 D1 主存储。

## 风险与边界
- **D1 单行走 blob 的并发 last-write-wins**：个人多设备极罕见；已用 pull-then-push 合并缓解。如需强一致，后续可升级为行级表（每记录一行）。
- **理论备份窗口**：两次自动备份之间（≤24h）若遇灾难，最多丢失当天增量；可把 backup.yml 的 cron 频率调高（如每小时）进一步缩小窗口。
- **D1 单值 1MB 上限**：本方案按账号拆分已规避；若某账号数据异常膨胀（如超大 GPS 轨迹），再按记录级拆分即可。
