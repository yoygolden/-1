# SwimTrack 部署教程（小鱼游泳 · 多设备云同步）

> 目标：把整个项目（前端页面 + Node 后端）部署到一个免费 Node 平台，得到一个**固定网址**。
> 手机用这个网址打开，就能注册/登录账号，在任何设备上查看同一份游泳成绩。

---

## 一、原理（先看，1 分钟）

- `server.js` 用同一个端口**既托管网页又提供 `/api` 接口**（同源）。
- 前端 `app.js` 里用的是相对路径 `/api/...`，所以**只要前端和后端在同一个网址下**，多设备登录直接能用，**不用改任何代码**。
- 部署就是把整个文件夹推到 Railway / Render，它们会运行 `node server.js` 并把你的网址指向它。

```
你手机  ──打开网址──▶  Railway/Render 上的 server.js
                           ├── 返回 index.html（前端页面）
                           └── 处理 /api/...（账号 + 成绩同步）
```

⚠️ **重要限制**：Railway / Render 的硬盘是**临时性的**。默认数据写在 `data/store.json`，
重启/重新部署后会被清空。解决办法见下文「持久化」一节。
（即便数据被清空也别慌：每台手机本地 `localStorage` 还有一份，"我的"页点"同步"即可恢复。）

---

## 二、准备：把代码推到 GitHub

这两个平台都从 **GitHub 仓库**拉代码。请先建一个空仓库，再把本地代码推上去。

> 本地代码已经 `git commit` 好了，只差推到远程。

1. 打开 https://github.com → 右上角 **New repository**
   - Repository name：`swimtrack`（随意）
   - 选 **Public**（免费）或 Private 都行
   - **不要**勾选 "Add a README"（保持空仓库）
   - 点 **Create repository**

2. 复制页面上给出的仓库地址，类似：
   ```
   https://github.com/你的用户名/swimtrack.git
   ```

3. 在本机项目目录执行（把地址换成你自己的）：

   ```bash
   cd swim-tracker
   git remote add origin https://github.com/你的用户名/swimtrack.git
   git branch -M main
   git push -u origin main
   ```

   推送成功后，GitHub 页面就能看到所有文件了。

---

## 三、方案 A：Railway 部署（推荐，支持永久磁盘）

Railway 免费额度对新项目较友好，且**支持挂载持久磁盘**，云端成绩真正不会丢。

### 1. 注册并新建项目
- 打开 https://railway.app → 用 **GitHub 登录**
- 进入控制台 → **New Project** → **Deploy from GitHub repo**
- 选中刚才的 `swimtrack` 仓库 → 确认部署

### 2. 等它部署完成
- Railway 会读 `railway.json`：构建跑 `npm install`，启动跑 `node server.js`
- 构建完，项目里会出现一个 **Web 服务**，状态变绿
- 点服务 → 顶部 **Settings → Domains → Generate Domain**
- 得到一个网址，类似：`https://swimtrack-production.up.railway.app`
  👉 **这个网址就是你的 App 地址，收藏/发给家人即可。**

### 3.（关键）开启持久化磁盘，避免数据丢失
1. 在 Railway 项目里，点你的服务 → **Settings → Volumes → Create Volume**
   - Mount Path 填：`/data`
   - 大小 1 GB 足够（几乎免费）
2. 同服务 → **Variables** → 新增变量：
   - `DATA_DIR` = `/data`
   （`server.js` 会优先使用这个目录存数据）
3. 改完变量会自动重新部署一次。之后云端成绩就写在 `/data`，**重启也不会丢**。

> 不挂磁盘也能用，只是数据在重启后可能清空（靠各手机本地备份可恢复）。
> 建议家庭长期使用还是挂上这 1GB 磁盘。

### 4. 使用
- 手机浏览器打开上面的 Railway 网址
- 「我的」页 → 云端卡片 → **注册**一个账号（比如 `xiaoming` + 密码）
- 在另一台手机/电脑打开**同一个网址**，用同一账号**登录**
- 任意一端新增成绩 → 「我的」点「同步」→ 另一端刷新即可看到
- 想放桌面快捷方式：手机浏览器菜单 →「添加到主屏幕」

---

## 四、方案 B：Render 部署（完全免费，但会休眠）

Render 免费档**不需要信用卡**，但：
- **15 分钟无访问会自动休眠**，下次打开要等几秒唤醒；
- **免费档没有持久磁盘**，休眠/重启后 `data/store.json` 会被清空。

适合"能接受偶尔重置、靠本地+导出备份"的轻量使用。

### 步骤
1. 打开 https://render.com → 用 **GitHub 登录**
2. 控制台 → **New + → Web Service**
3. 连接 `swimtrack` 仓库
4. Render 会自动读取仓库里的 `render.yaml`，配置基本不用改：
   - Runtime：`Node`
   - Build Command：`npm install`
   - Start Command：`node server.js`
   - Plan：**Free**
5. 点 **Create Web Service**，等待部署
6. 部署完会给一个网址，类似：`https://swimtrack.onrender.com`
   👉 这就是你的 App 地址。

### 使用注意（Render 免费档）
- 云端数据可能因休眠被清空 → 把**每台手机本地**当作主数据，**定期在"我的"页点"导出"**做备份。
- 云端清空后，任意一端点"同步"即可重新把数据推回云端。
- 如果以后想让数据永久保存，建议换到方案 A（Railway + 磁盘）。

---

## 五、原本的 CloudStudio 链接还要不要？

- CloudStudio 那个旧链接（`*.agentos-app.net`）是**纯静态**的，**不支持多设备云登录**，
  只能当"本地存储版"用（数据存在打开它的那台手机上）。
- 部署成功后，**主用 Railway/Render 给的网址**即可，它功能最全。
- 你也可以在 CloudStudio 里把前端 `app.js` 的接口地址改指向新后端，但**没必要**——
  因为整套一起部署后，新网址已经前后端同源、开箱即用。

---

## 六、常见问题

**Q：部署后打开网页显示 "云端服务暂不可用"？**
A：说明前端没和后端同源。请确认你打开的是**部署平台给的网址**（Railway/Render 域名），
而不是 CloudStudio 旧链接。整套一起部署时前后端同域，不会出现这个提示。

**Q：忘记密码怎么办？**
A：目前后端没有"找回密码"。可在"我的"页「导出」备份，然后重新注册一个账号再「导入」。

**Q：数据存在哪里最安全？**
A：三层保险——(1) 每台手机本地 `localStorage`；(2) 云端账号；(3) 手动"导出"下载的备份文件。
清微信缓存/重装微信会清掉 (1)，所以 (2)(3) 很重要。

**Q：想换平台 / 删掉重来？**
A：直接在 Railway/Render 控制台删掉项目即可，不影响你手机本地的数据。
